scalar Date
scalar UUID

directive @auth(roles: [String]) on FIELD_DEFINITION

type HealthCheck {
  ok: Boolean!
  uptime: Float
}

type Query {
  health: HealthCheck
}

type Mutation {
  _empty: Boolean
}
